import 'package:flutter/material.dart';

class ImageGenerationPage extends StatelessWidget {
  const ImageGenerationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Image Generation Page'),
      ),
    );
  }
}
